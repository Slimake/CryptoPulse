## Requirement
git clone https://github.com/Slimake/CryptoPulse.git  
cd CryptoPulse  
python -m venv venv-cryptopulse  
pip install -r requirements.txt

create .env file in the root directory, add  
SECRET_KEY= key can be any valid string and  
X-ACCESS-KEY= key should be gotten from NEWSDATA.IO
